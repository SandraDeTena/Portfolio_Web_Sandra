name: Build and Deploy Portfolio
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🏗️
        run: |
          npm install
          npx ng build --configuration production --base-href "/Portfolio_Web_Sandra/"

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist/portfolio-sandra/browser
          branch: gh-pages